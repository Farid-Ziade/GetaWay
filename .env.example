# Copy this file to `.env` for local development only.
# Never commit `.env` or put API keys in source code.
#
# Example keys you may add later (backend URL, feature flags, etc.):
# GETAWAY_API_BASE_URL=https://your-api.example.com
